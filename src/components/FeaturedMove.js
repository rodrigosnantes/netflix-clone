import React from 'react';
import './FeaturedMove.css'

export default function FeaturedMove({ featuredData }) {
    console.log(featuredData)
    return (
        <div>Filme em destaque</div>
    )
}